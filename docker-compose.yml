services:
  fba_frontend:
    image: ghcr.io/moi-igit/vue-demo:latest
    container_name: fba_frontend
    restart: always
    ports:
      - "80:80"
    networks:
      - fba_network

networks:
  fba_network:
    external: true
