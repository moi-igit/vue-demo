所有函数必须添加简洁的中文注释，说明其用途；Vue 模板中的主要结构块需要添加中文注释，说明页面区域含义。避免为显而易见的单行代码添加冗余注释。

Git 提交信息采用 Conventional Commits 格式：`<type>(optional-scope): 简短中文说明`。常用 type 包括 `feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`build`、`ci`、`chore`。示例：`build(deps): 引入 Element Plus`、`docs(agents): 优化注释规范`。
